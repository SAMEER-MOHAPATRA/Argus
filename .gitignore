__pycache__/
*.pyc
*.pyo
.venv/
.pytest_cache/
.tokensave/
# ignored so the public template never carries personal data; the Action
# force-adds it in your private copy (see .github/workflows/discover.yml)
jobs_found.csv
jobs_applied.csv
*.env
logs/
*.bak
jobs_archive.csv
jobs_found.json
dashboard.html
.claude/skills/
scratchpad/
graphify-out/
# local planning notes; config.personal.toml lives here and must stay private
plans/
# files parked by the cleanup; delete the folder when you are sure
To Delete/
